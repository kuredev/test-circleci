version: 2.1
commands:
  say_hello:
    steps:
      - run: echo "hello from commands"
jobs:
  job1:
    docker:
      - image: circleci/ruby:2.6.5
    steps:
      - checkout
      - run: echo "hello world"
      - say_hello

  job2:
    docker:
      - image: circleci/ruby:2.6.5
    steps:
      - checkout
      - run: echo "hello world"
      - say_hello
workflows:
  version: 2
  build:
    jobs:
      - job1
      - job2

    
